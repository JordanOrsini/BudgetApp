import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerView: {
    flex: 1,
    alignItems: 'center',
    paddingTop: 50,
  },
  headerText: {
    fontSize: 50,
    fontWeight: 'bold',
  },
});